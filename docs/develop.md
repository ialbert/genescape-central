

Linting:

    hatch run lint:all

Reformatting and fixing linting errors:

    hatch run lint:fmt

Running tests:

    hatch run test

Building the package:

    hatch build

Publish to PyPI:

    hatch publish
